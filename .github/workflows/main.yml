name: CI
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
     
    steps:
    - name: host
      run: ${{ secrets.HOST_TEST }} ${{ secrets.USER_NAME }} ${{ secrets.USER_PASSWORD }}
      
    - name: cehis
      uses: appleboy/ssh-action@v0.1.10
      with:
        host: 49.232.195.173
        username: root
        password: qweASD@123
        script: cd /data/wwwroot/ceshi && git pull
        
